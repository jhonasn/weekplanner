MDScreen:
    MDFloatLayout:
        MDLabel:
            id: title
            halign: 'center'
            markup: True
            text: "[u][size=15][b]Segunda feira:[/b][/size][/u]"
            pos_hint: {'y': .47}

        #MDScrollView:
            ##id: box
            #pos_hint: {'y': 0, 'center_x': .5}
            #size_hint: .9, .94

            #MDBoxLayout:
                #id: box
                #spacing: "10dp"
                #adaptive_size: True
                ##pos_hint: {"center_x": .5, "center_y": .5}
                #orientation: "vertical"

        MDFloatingActionButton:
            md_bg_color: app.theme_cls.primary_color
            #icon: "pen-plus"
            icon: "plus-thick"
            type: "standard"
            elevation_normal: 12
            pos_hint: {'x': .75, 'y':.04}
            on_release: app.add_event()
